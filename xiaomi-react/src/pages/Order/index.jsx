import React, { Component } from 'react';
import style from './index.module.css';

class Order extends Component {
        render() {
                return (
                        <div className={ style["page-wrapper"] }>这是订单页</div>
                );
        }
}

export default Order;